import * as React from "react"
import { HTMLMultiwayUIProps } from '@multiway-ui/system'

export interface Use{{capitalize uiName}}Props extends HTMLMultiwayUIProps<'div'> {
  /**
   * Ref to the DOM node.
   */
  ref?: React.RefObject<HTMLElement | null> | React.MutableRefObject<HTMLElement | null> | React.Ref<HTMLElement | null>
}

export function use{{capitalize uiName}}(props: Use{{capitalize uiName}}Props) {
  const { ref, as, ...otherProps } = props

  const domRef = React.useRef(null)

  React.useImperativeHandle(ref, () => domRef.current)
  const Component = as || 'div'

  return { Component, domRef, ...otherProps }
}

export type Use{{capitalize uiName}}Return = ReturnType<typeof use{{capitalize uiName}}>