import * as React from "react"
import { HTMLFaizUIProps } from '@faiz-ui/system'

export interface Use{{capitalize componentName}}Props extends HTMLFaizUIProps<'div'> {
  /**
   * Ref to the DOM node.
   */
  ref?: React.RefObject<HTMLElement | null> | React.MutableRefObject<HTMLElement | null> | React.Ref<HTMLElement | null>
}

export function use{{capitalize componentName}}(props: Use{{capitalize componentName}}Props) {
  const { ref, as, ...otherProps } = props

  const domRef = React.useRef(null)

  React.useImperativeHandle(ref, () => domRef.current)
  const Component = as || 'div'

  return { Component, domRef, ...otherProps }
}

export type Use{{capitalize componentName}}Return = ReturnType<typeof use{{capitalize componentName}}>